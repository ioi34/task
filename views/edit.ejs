<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <form action="/edit" method="post">
        <div>
            <label>title</label>
            <input type="text" value="<%=data._id %>" name="id">
            <input type="text" value="<%=data.title %>" name="title">
        </div>
        <p></p>
        <div>
            <label>content</label>
            <textarea name="content" rows="10"><%= data.content %></textarea>
        </div>
        <p></p>
        <div>
            <label>date</label>
            <input type="date" value="<%=data.date %>" name="someDate">
        </div>
        <p></p>
        <button type = "submit">수정</button>
    </form>
</body>
</html>