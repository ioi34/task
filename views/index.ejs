<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <%- include('navbar', { user: user }) %>

    <h1>홈입니다.</h1>

    <% if(user){ %>
    <h3>반갑습니다. <%=user.userid %>님</h3>
    <a href ="/logout">로그아웃</a>
    <% }else{%>
    <h3>로그인 해주세요</h3>
    <p></p>
    <button><b><a href="/login">로그인</a></b></button>
    <a href="/signup">회원가입</a>
    <% }%>
</body>
</html>